export interface Article {
    title: string
    author: string
    description: string
    // image: string // urlToImage
    url: string
}

export interface Country {
    name: string
    code: string
    flag: string
}